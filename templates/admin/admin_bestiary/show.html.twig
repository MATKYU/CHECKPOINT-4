{# idk  #}

{# {% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Bestiary{% endblock %}

{% block body %}
    <h1>Bestiary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bestiary.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ bestiary.image }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bestiary.name }}</td>
            </tr>
            <tr>
                <th>Descritpion</th>
                <td>{{ bestiary.descritpion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_bestiary_index') }}">back to list</a>

    <a href="{{ path('app_admin_bestiary_edit', {'id': bestiary.id}) }}">edit</a>

    {{ include('admin_bestiary/_delete_form.html.twig') }}
{% endblock %} #}
