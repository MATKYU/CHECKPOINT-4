<div class="ms-1 border border-3 border-primary p-3">
	{{ form_start(form) }}
	{{ form_row(form.image) }}
	{{ form_row(form.name) }}
	{{ form_row(form.description) }}
	<button class="btn btn-secondary btn-lg m-2 text-light">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}
</div>
